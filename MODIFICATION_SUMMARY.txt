1150713 系統專題審查報告－程式修正版
完成日期：2026-07-21
依據：1150713_系統專題審查報告 (1).docx

本次已完成：
1. 移除 public/server.js，後端程式不再被 Express 靜態公開。
2. 清除 lib/lib、test/test、舊 env 與未使用程式等重複或混淆檔案。
3. 統一 index.html、login.html、dashboard.html 等正式檔名與 PWA 路徑。
4. Service Worker 改為只快取靜態資產；登入頁、儀表板、API 與健康檢查不離線快取。
5. defects、summary、predict、AI、systems、site-config、current-product 全面套用 JWT 與 tenant/system scope。
6. E-stop 限制 tenant_admin/super_admin，加入 system_id、command_id、機台 Topic、ACK 與 AuditLog。
7. 新增 GET /api/estop/:command_id，可查詢急停 ACK 狀態。
8. OTP 存入 MongoDB TTL collection，加入重寄冷卻、有效期限與錯誤次數上限。
9. 註冊加入速率限制、密碼政策、公開註冊開關與可選邀請碼。
10. 加入 Helmet、CSP、CORS allowlist、request id、請求大小限制與統一錯誤處理。
11. MQTT payload 驗證 system_id、id/case_id、OK/NG、timestamp、HTTPS 圖片路徑與單次筆數。
12. NG 警報改用 MongoDB 時間窗統計與冷卻，避免服務重啟後歸零。
13. 管理 collection API 採白名單、分頁並排除 password/token/secret 等敏感欄位。
14. 新增 AuditLog 查詢 API、瑕疵 CSV 匯出 API，事件紀錄頁增加「匯出 CSV」按鈕。
15. 修正事件紀錄、產品標籤與 NG 圖片等主要動態輸出點，避免把資料直接塞入 innerHTML/onclick。
16. 新增資料庫索引、Node 單元測試與 GitHub Actions CI。
17. 統一 .env.example、render.yaml 與 README_DEPLOY.md。
18. npm audit --omit=dev：0 個已知漏洞。

檢查指令：
- npm ci --no-fund
- npm run check
- npm audit --omit=dev

Render：
- Build Command：bash render-build.sh
- Start Command：npm start
- Health Check：/health

仍屬後續架構升級：
- JWT 目前仍存 sessionStorage；完整改成 HttpOnly Secure SameSite Cookie 需同步重構所有前端登入與 API 呼叫。
- 舊頁面仍有大量 inline script/style，因此 CSP 暫時保留 unsafe-inline；主要外部資料輸出點已先安全化。
- E-stop ACK 必須由實際設備訂閱與回傳，單靠網站無法模擬硬體已執行。
